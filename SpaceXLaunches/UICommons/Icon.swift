import UIKit

extension UIImage {
    enum Icon {
        static let success = UIImage(imageLiteralResourceName: "success")
        static let failure = UIImage(imageLiteralResourceName: "failure")
        static let unknown = UIImage(imageLiteralResourceName: "unknown")
    }
}
